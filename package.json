{
  "name": "@e4a/irmaseal-mail-utils",
  "version": "0.0.1",
  "description": "Irmaseal mail utils library providing functionality to read and compose Irmaseal MIME mails",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@types/node": "^15.12.5",
    "prettier": "^2.4.0",
    "typescript": "^4.4.2"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && tsc",
    "test": "npm run build && cd test && tsc *.ts && node composeAndReadMail.js && node readExchangeMail.js",
    "bundle": "npm run build && bundle-js dist/composeMail.js  -o dist/bundleMailUtils.js",
    "format": "prettier --write ./src"
  },
  "dependencies": {}
}
